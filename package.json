{
  "name": "vietnamese-sign-language-web",
  "version": "1.0.0",
  "description": "Vietnamese Sign Language Learning Platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others-on-fail -n \"AI,Backend,Frontend\" -c \"magenta,cyan,green\" \"npm run dev:ai\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:ai": "cd aiservice && python -m uvicorn api:app --host 0.0.0.0 --port 8000 --reload",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}